name: remote ssh command
on: push
#   pull_request:
#     types:
#       - closed
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using ssh key
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.KEY }}
        port: ${{ secrets.PORT }}
#         script: /home/woodylin/publish.sh
        script: /home/woodylin/test.sh
